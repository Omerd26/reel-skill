# איך מפרסמים ומחלקים — צעד אחר צעד

*(הקובץ הזה בשבילך, לא בשביל המשתמשים. אפשר למחוק אותו מהריפו הציבורי.)*

## שלב 1 — להעלות ל-GitHub (פעם אחת, 3 דקות)

1. היכנס ל-https://github.com/new
2. שם הריפו: **`reel-skill`** · תיאור: `עורך רילים בעברית — סקיל חינמי ל-Claude Code`
3. **Public** · **בלי** לסמן README/gitignore/license (כבר יש לנו)
4. Create repository
5. חזור לטרמינל והרץ (החלף `omerdraizin` אם שם המשתמש שלך שונה):

```bash
cd "~/Downloads/omer digital/dredit-skill"
git remote add origin https://github.com/omerdraizin/reel-skill.git
git branch -M main
git push -u origin main
```

GitHub יבקש התחברות — פשוט תאשר בדפדפן שייפתח.

**אם שם המשתמש שלך שונה מ-`omerdraizin`** — עדכן את הקישור גם ב-`README.md`
(שורת ה-`git clone`) לפני ה-push, אחרת המשתמשים יקבלו 404.

## שלב 2 — לבדוק שזה עובד למישהו אחר

פתח את הקישור בחלון גלישה בסתר: `https://github.com/<שמך>/reel-skill`
ותוודא שאתה רואה את ה-README בעברית. זה מה שהעוקבים יראו.

## שלב 3 — לעדכן מתי שרוצים

```bash
git add -A && git commit -m "שיפור" && git push
```

המשתמשים מקבלים את העדכון עם `git pull` בתיקייה שלהם.

---

## הודעת ManyChat

**כשמישהו מגיב במילת המפתח:**

> היי! הנה העורך 🎬
>
> זה סקיל חינמי שהופך קליפ שאתה מדבר למצלמה לריל ערוך — כתוביות בעברית,
> גרפיקות, מוזיקה. רץ אצלך במחשב, בלי הרשמה ובלי תשלום.
>
> הקישור + הוראות:
> github.com/omerdraizin/reel-skill
>
> צריך Claude Code (מנוי Claude). ההתקנה היא 2 פקודות והסקריפט עושה את השאר.
>
> תקוע? תכתוב לי כאן ואני אעזור.

**גרסה קצרה יותר (אם ManyChat מגביל אורך):**

> הנה העורך 🎬 github.com/omerdraizin/reel-skill
> קליפ שלך → ריל ערוך עם כתוביות וגרפיקות. חינם, רץ על המחשב שלך.
> צריך Claude Code. תקוע? כתוב לי.

---

## מה לצפות שישאלו (ותשובות מוכנות)

**"צריך לשלם על משהו?"**
רק מנוי Claude שכבר יש לך (או Pro ב-20$). הסקיל עצמו חינם ואין קרדיטים.

**"זה בטוח? הסרטונים שלי לא עוברים לאף אחד?"**
הכול רץ על המחשב שלך. שום קובץ לא נשלח לשום שרת.

**"אין לי מק"**
עובד גם בווינדוס — צריך רק להתקין ffmpeg עם `winget install ffmpeg`.

**"זה לא עובד לי"**
תבקש צילום מסך של השגיאה. 90% מהמקרים: Node לא מותקן, או שהם לא הריצו
`./install.sh`.

**"אפשר באנגלית?"**
כן — פשוט תגיד לסקיל "the video is in English".
